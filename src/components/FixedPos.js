import React from "react";
import {withStyles} from "@material-ui/core";

const styles = {
  pos: {

  }
};


function FixedPos(props) {
    return(
        <div>

        </div>
    );
}

export default withStyles(styles)(FixedPos);